#!/bin/sh
# Uses .venv (mido for MIDI) when it exists, else the system python3.
here="$(dirname "$0")"
py=python3
[ -x "$here/.venv/bin/python3" ] && py="$here/.venv/bin/python3"
exec "$py" "$here/tools/djmono.py" "$@"
